package org.kt.hw.controller;
/*
import lombok.extern.slf4j.Slf4j;
import org.kt.hw.model.Health;
import org.kt.hw.model.HealthStatus;
import org.kt.hw.model.InfoStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RestController;

@RestController
//@RequestMapping("/api/v1")
@Slf4j
public class HealthController {

    @RequestMapping(value = "/health", method = RequestMethod.GET)
    public ResponseEntity<Health> getHealth() {
        //log.debug("REST request to get the Health Status");
        final var health = new Health();
        health.setStatus(HealthStatus.OK);
        return ResponseEntity.ok().body(health);
    }

    @RequestMapping(value = "/", method = RequestMethod.GET)
    public ResponseEntity<InfoStatus> getInfo() {
        //log.debug("REST request to get the Info Status");
        final var info = new InfoStatus("Java, Spring Boot and Postgres API");
        return ResponseEntity.ok().body(info);
    }
}
 */
